# BC PHO RAG

Learning-focused RAG project in C# using BC Provincial Health Officer PDFs.

Current status:
- project docs are in `docs/`
- raw source PDFs are in `data/raw`
- solution scaffold is in `src/`
